// Sample 5: Complete Program with Multiple Features
#Incorporate <timeline.h>
#Constant MAX_SIZE 100

structure Point {
    Energy x;
    Energy y;
};

Matter factorial(Matter n) {
    Era (n <= 1) {
        Resolve 1;
    }
    Resolve n * factorial(n - 1);
}

Event main() {
    // Variable declarations
    Matter numbers[10];
    Energy temperature = 98.6;
    Atom initial = 'A';
    Stream message = "ChronoScript is running";
    Truth isActive = 1;
    
    // Mathematical operations
    Energy result = sine(45.0) + cosine(30.0);
    Matter absValue = absolute(-42);
    Energy sqrtValue = squareroot(16.0);
    
    // Bitwise operations
    Matter a = 5;
    Matter b = 3;
    Matter bitwiseAnd = a & b;
    Matter bitwiseOr = a | b;
    Matter shifted = a << 2;
    
    // Control structures
    Era (temperature > 100.0) {
        Broadcast("Too hot!");
    } Alternate Era (temperature < 32.0) {
        Broadcast("Too cold!");
    } Alternate {
        Broadcast("Just right!");
    }
    
    // Loops
    Loop (Matter i = 0; i < 5; i = i + 1) {
        Era (i == 2) {
            Persist;
        }
        numbers[i] = i * i;
    }
    
    // Function calls
    Matter fact5 = factorial(5);
    
    // Logical operations
    Era (isActive && temperature > 0.0) {
        Broadcast("System operational");
    }
    
    Resolve 0;
}
